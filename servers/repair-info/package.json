{
  "name": "@cbaileydev/repair-info-mcp",
  "version": "0.1.0",
  "description": "MCP server for NHTSA recalls, complaints, safety ratings, and generic maintenance schedules.",
  "type": "module",
  "bin": {
    "repair-info-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "smoke": "SMOKE=1 vitest run src/smoke.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
