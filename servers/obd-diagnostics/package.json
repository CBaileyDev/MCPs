{
  "name": "@cbaileydev/obd-diagnostics-mcp",
  "version": "0.1.0",
  "description": "OBD-II diagnostics MCP server: normalize scan logs and (mocked) adapter evidence into DTCs, readiness, freeze frames, and PIDs. Read-only v1.",
  "type": "module",
  "bin": {
    "obd-diagnostics-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
